# Copia este archivo como .env.local (no se sube al repo).
# Valores del proyecto de Supabase (en desarrollo: proyecto de JCA Soft).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# El backend se consume en la misma URL, bajo /api (Next lo redirige al API interno).
# No hace falta configurar la URL del API: siempre se llama como /api/...
