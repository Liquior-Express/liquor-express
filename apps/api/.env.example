# Copia este archivo como .env y completa los valores.
# El .env real NO se sube al repositorio (está en .gitignore).

# Puerto INTERNO del API (Next redirige /api hacia aquí). No usar el mismo que Next.
API_PORT=4000

# Supabase (base de datos). En desarrollo: proyecto propio de JCA Soft.
# En producción: proyecto del cliente (JCA entra como colaborador).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
